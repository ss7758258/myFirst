<!-- 导航 -->
<nav opts="{{navConf}}"></nav>
<view class="panel">
    <view class="photo">
        <image src="{{iconPath}}"></image>
        <text class="text">{{nickName}}</text>
    </view>

    <!-- 运势提醒 -->
    <view class="flex notice">
        <view>
            <image class="notice_icon" src="{{imgs.notice_icon}}"></image>
        </view>
        <view>运势提醒</view>
        <view bindtap="handleRemindToggle">
            <view class="switchOn {{isFlag ? '' : 'off'}}">
                <view>
                    <view></view>
                </view>
            </view>
        </view>
    </view>

    <!-- 小星星 -->
    <!-- <view class="flex card_record"  bindtap="handleWalletClick">
        <view>
            <image class="notice_icon" src="{{imgs.wallet}}"></image>
        </view>
        <view>小星星</view>
        <view>
            <image class="notice_icon" src="{{imgs.right}}"></image>
        </view>
    </view> -->


    <!-- 每日打卡-->
    <!-- <view class="flex card_record" wx:if="{{clockStatus}}" bindtap="handleClockinClick">
        <view>
            <image class="notice_icon" src="{{imgs.daka}}"></image>
        </view>
        <view>每日打卡</view>
        <view>
            <image class="notice_icon" src="{{imgs.right}}"></image>
        </view>
    </view> -->

    <!-- 我要吐槽 -->
    <view class="flex opinion"  bindtap="handleFeedbackClick">
        <view>
            <image class="notice_icon" src="{{imgs.opinion}}"></image>
        </view>
        <view>我要吐槽</view>
        <view>
            <image class="notice_icon" src="{{imgs.right}}"></image>
        </view>
    </view>
</view>